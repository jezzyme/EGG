# 웹 서버
Flask>=3.0.0
Werkzeug>=3.0.0
gunicorn>=21.2.0

# 데이터베이스 (배포 시 PostgreSQL, 로컬은 표준 라이브러리 sqlite3)
psycopg[binary,pool]>=3.1.0

# 자기소개서 파일 읽기
pypdf>=4.0.0
olefile>=0.46

# 선택: 브라우저 받아쓰기를 못 쓰는 환경에서 녹음 파일을 서버가 분석하려면 아래를 설치한다.
# 메모리를 많이 써서 무료 호스팅에서는 켜지 않는 것을 권장한다.
# faster-whisper>=1.1.0
